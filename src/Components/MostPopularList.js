import React from "react";

function MostPopularList() {
    
    return (
        <div className="p-4">
          <h2 className="text-lg font-semibold mb-2">Usuarios Populares</h2>
          <ul>
            {/* Futura lista de usuarios populares (dejo esto en blanco por ahora) */}
          </ul>
        </div>
      );
}

export default MostPopularList;